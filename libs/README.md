# Libs

Reusable code shared across applications.

## Structure

- [`shared`](./shared/README.md) - cross-app shared libraries

## Guidelines

- Keep libraries focused and small.
- Respect enforced tags/module boundaries.
- Move shared contracts and pure helpers to `libs/shared/*`.
